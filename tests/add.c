#include <stdio.h>
#include <stdlib.h>

int main(int argc, char *argv[]) {
  unsigned int a = 100;
  int b = -10;
  int c = a*b;
  long long * p,*q,*r;
  double pp;
  p = *p*a;
  p = p + 10;
  r = p+*q;

  c = a & (int)pp;
  return 0;
}
